name = "vanshul-ctx"
account_id = "f4eea2378d0363cc74b2c7b63d4da331"
main = "src/worker.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# Serves the static landing page in public/ for any path that isn't /mcp or /health.
[assets]
directory = "./public"

# Custom domain — Cloudflare creates the DNS record + cert automatically.
[[routes]]
pattern = "ctx.vanshul.com"
custom_domain = true
